# Architecture impact — every measured finding, and what it changed

**Date: 2026-07-25 (#527).** Population: every check registered in `tools/vendor-verify/verify.py`.
Recomputed by `pixi run audit-completeness`, which fails if a check has no row here.

This is step 5 of the audit chain: *what has reality changed for the architecture?* The answer is
only checkable if the question is asked of **every** measurement, including the ones that changed
nothing — otherwise "I considered the findings" is an assertion. A `no impact` row is a real
disposition and the commonest correct one: most of these confirm something the design already
assumed, and a confirmation is worth exactly as much as it costs to re-run.

Read alongside [`decision-audit.md`](decision-audit.md), which does the same over decision records.
This table runs the other direction: from measurement to design.

## The gate

| check | result | architectural impact |
|---|---|---|
| `gate.allowedtools-is-preapproval-not-ceiling` | `--allowedTools` pre-approves and does not restrict | **Largest impact in the audit.** Tool restriction is not a capability boundary — a model denied `Write` writes through `Bash`. Forced [0029](decisions/0029-the-gate-is-three-mechanisms.md); made a `PreToolUse` hook mandatory on every worker; moved gate work from M28 into M26; flipped journey J6 from Partial to **Fails**. Live defect #529. |
| `gate.hook-exit-2-beats-allow` | a hook exiting 2 blocks despite an allow rule | The enforcement point 0029 rests on. Without it the hook is advisory and nothing covers vendor tools. |
| `agy.adapters-own-flag-set-still-gates` | the same question on agy, whose hook rides `.agents/hooks.json` under `--add-dir` rather than `--settings` | **Stops the gate story being claude-only without saying so.** Every other gate measurement behind #703/#549/#550 ran claude, while the invariant is written about "a vendor CLI worker" — the `claim-scope` gate's exact failure, in the PR that cites it. It matters more here than on claude: `agy.permissions-are-global-only` makes the hook agy's *only* project-scoped gate, so nothing sits behind it. Its first run also exposed a confound in the check rather than a product defect — an escape target under `%AER_ARTIFACTS_ROOT%` is inside a directory the vendor was explicitly granted. |
| `gate.adapters-own-flag-set-still-gates` | a write escaping the outbox is blocked under the argv `ClaudeWorkerAdapter` really builds | **Closes the loop every prior gate finding was found outside.** `--bare` (#521), its two documented siblings, and environment inheritance (#549) were each found by READING, because nothing ran AER's own assembled flag set past the vendor. A hand-picked flag list structurally cannot find the next one — the suppressing flag is by definition the one the author omitted. `tools/Aer.GateProbe` emits the real adapter's resolved invocation so the check runs *that*. Verified discriminating: suppressing `--settings` in `Resolve` turns it red, with the write landing outside the outbox. |
| `agy.hook-command-survives-a-metacharacter-in-its-path` | which command-string forms let an agy hook handler actually start under `cmd /c` — production's own bare form, plain and with a space, and both quoted forms pinned dead | **Found that AER's mandatory gate had never fired on agy at all (#706), then found the first fix was equally dead (#710).** The adapter shipped `dotnet "<path>" agy-hook-check`; `cmd` does not strip quotes of either kind, so the handler never resolved, produced no stdout, and `agy.hook-malformed-stdout-fails-open` reads that as an **allow** — decision 0029's mandatory hook absent from every agy worker, silently, while six `agy.*` hook checks passed. All six ran their handler through an `sh` wrapper, so none exercised production's form — and `sh` strips single quotes itself, which is how #706's "fix" was chosen: it measured the wrapper. `claim-scope` from the inside, twice. What ships now is the bare path (8.3-shortened when spaced), and the check reads the command out of the hooks.json the real adapter writes rather than restating it. Pins a vendor behaviour AER cannot control and would not otherwise notice changing. |
| `agy.hook-deny-holds-under-the-mode-production-uses` | an agy hook deny still blocks under `--mode accept-edits`, the flag production defaults to | **Retires a cross-mode inference 0029 forbids.** Every other agy hook arm ran `--dangerously-skip-permissions`, which production reaches only when shell *and* network are both granted — so the gate's behaviour under the mode it normally runs in was assumed, on a vendor where `agy.force-ask-defeated-by-skip` is direct evidence that hook-verdict semantics *change* between skip and non-skip modes. Measured: it blocks under both, with the skip arm as the control. |
| `gate.simple-mode-override-restores-the-hook` | `CLAUDE_CODE_SIMPLE=0` fires the hook; an inherited `=1` kills the run at `Not logged in` rather than ungating it | **Retired an admission that was load-bearing.** `ClaudeWorkerAdapter`'s override was shipped as "best-effort, not a measured sentinel", its value chosen by analogy to a sibling variable — so 0029's mandatory hook rested on an unproven parse. Now measured. It also **corrected the hazard's shape**: an inherited `=1` fails loudly on a subscription host instead of producing a silent ungated worker, which is the opposite of what #549's threat model assumed. Scoped to subscription auth; an API-key host is unmeasured and is where the quiet failure could still live. |
| `gate.broken-hook-fails-open` | a hook that cannot execute lets the tool run, and the CLI reports nothing | **Turned 0029's startup self-check from prudence into a requirement**, and fixed what it must assert: a side effect the hook produced, per worker spawn. Also corrected the *cause* AER expected — CRLF and spaces in paths survive, so the vendor's documented Git Bash failure mode is not the one that bites, and the obvious mitigation would have prevented nothing. Closes #530. |
| `gate.hook-ask-in-auto` | a hook's `ask` forces a prompt even in `auto` | **Softened a conclusion 0015 had already drawn.** 0015 said an operator's `auto` leaves AER with no permission surface; the hook is the recovery path, so the surface survives a mode AER does not control. |
| `gate.elicitation-capability` | uncircumventable on claude across all three modes | Portable refusal primitive. With the agy arm, it is the only gate primitive measured unbypassable on both vendors. |
| `agy.elicitation-capability` | uncircumventable on agy under skip-permissions | **Turned an inference into a measurement.** Without it 0029's portability row would have been "assumed" — and `agy.force-ask-defeated-by-skip` shows that exact inference failing for the neighbouring mechanism. |
| `agy.url-mode-elicitation` | agy declares and routes SEP-1036 `mode: "url"` | The non-blocking durable gate is **already standardized**, and exists on one vendor. Added 0029's "build it to migrate" clause: releasing the call must be the normal path, not the crash path. |
| `gate.requires-user-interaction` | no mode or allow rule approves it | Confirms the vendor extension is a real refusal — but it is claude-only and hard-denies headless, which is why 0029 does not build on it. |
| `gate.prompt-tool-conversion` | a prompt tool's `allow` becomes `deny` for such a tool | Bounds what `--permission-prompt-tool` can approve. No design change: AER never needed to approve one. |
| `gate.ask-rule-beats-bypass` | an explicit `ask` rule gates under `bypassPermissions` | **No impact** — confirms 0004's fail-closed premise. Kept because it is the cheapest sentinel for a regression in the permission ladder. |
| `gate.add-dir-loads-no-config` | `--add-dir` grants files, loads no config | Launch constraint: AER must control the worker's cwd or pass `--settings`. Lands in M26, not M28. |
| `gate.permission-request-not-headless` | `PermissionRequest` never fires under `-p` | Removed 0018's assumed notification source. Forced [0030](decisions/0030-aer-is-its-own-notifier.md). |
| `gate.headless-event-surface` | 10 events fire, 3 silent, 10 untested | The other half of 0030 — it is what makes "then what?" answerable, and its three-way split is why the record does not assert eleven untested things. |
| `gate.elicitation-hook-event-fires` | the `Elicitation` hook event **does** fire under `-p` | **No impact, and it is the one positive in the group.** It closes 0030's last untested row — the earlier zero came from a run with no MCP server registered, so no elicitation could occur. For AER's own gate the event is redundant: AER *is* the server and holds the pause already. It would matter for a pause AER did not author, but whether a third-party server can even be loaded alongside `--mcp-config` is unresolved (the run's server list did not read, and `None` is recorded as not-observed rather than folded into "zero servers"). Recorded to the measured scope, not to the scenario that motivated the check. |
| `effort.agy-rejection-is-per-model` | **run 2026-07-28.** `gemini-3-pro` fails with no `--effort` at all, naming the model | **Retired an inference this register was carrying.** The error that reads `--effort is not supported for model X` was never about `--effort` — X was simply not a model. Nothing establishes per-model effort support, so any surface built on "this model does not support effort" was building on a misread. The stderr pin in `OutcomeClassifierTests` is unaffected: it pins what AER must surface, not why agy said it. |
| `effort.agy-effort-and-suffix-must-agree` | **run 2026-07-28, and it replaced the question.** A disagreeing pair is refused at bind time; an agreeing pair runs | Asking which control *wins* was the wrong question — there is no precedence. `--model gemini-3.6-flash-low --effort high` returns `conflicts with --effort=high` before any run, while `gemini-3.1-pro-high --effort high` answers. So the two are **one control with two spellings**, and a surface offering them independently builds an invocation the vendor refuses after the operator has already waited. Sentinel: if agy ever starts resolving the conflict silently, a UI keeping them in sync would be over-constrained with nothing to say so. This also narrowed the register's older "accepts both together" reading, which had been generalised from an agreeing pair. |
| `gate.sessionstart-without-a-turn` | **run 2026-08-03 (#948), PASS-with-scope.** No invocation shape produced a successful run reporting `num_turns: 0` — every shape that emits valid JSON also took a turn. But `SessionStart` fired on every zero-content shape tried, including one (`no -p` at all) that structurally cannot reach a model: the CLI rejects it pre-flight ("Input must be provided...") with `SessionStart` already fired. A resumed session remains untested | Decides #532's shape before it is built. That issue argues for probing the mandatory gate on `SessionStart` rather than `PreToolUse` "at zero model cost", citing `gate.headless-event-surface` — which measured only that the event *fires*, inside a run that took a full turn and is in `NEEDS_CAPABILITY` for that reason. This run answers the question `gate.headless-event-surface` didn't: `SessionStart` reliably precedes any invocation that could possibly take a turn, which is the practical claim #532 needs even though no arm cleanly reported a billed zero. See [vendor-doc-audit.md](vendor-doc-audit.md) § "Proving the gate fired is asymmetric". |
| `gate.agy-toolcall-injection-does-not-work` | **run 2026-08-03 (#948), FAIL (measured negative), `PreInvocation` only.** Injecting `{"toolCall": {...}}` from `PreInvocation` kills the run (`unknown injected step type: <nil>`) — checked-in, re-runnable. A one-time manual run (same session, not re-verified by this check) found the identical payload from `PostInvocation` hits the same internal error, non-fatally; disclosed in the check's own docstring as a corroborating observation, not an equally-measured claim. No changelog record of `toolCall` injection ever working on either hook point. | Closes the "obvious rescue" `vendor-doc-audit.md` had left open for #532's agy half. `injectSteps.toolCall` is documented but not implemented in the installed CLI — measured on 1.1.9, re-confirmed on 1.1.10 after agy auto-updated mid-session, same result both times — not an unlucky invocation shape, the vendor's own code does not recognise a field its own docs list. **agy has no free way to prove the gate fired**: any proof costs at least one real, model-driven tool call, same as if AER simply asked the model to call one. This closes the only theoretical zero-cost path on agy; #532's design for that vendor now has to either accept a per-verification model cost or fall back to a proof-once-per-config-change cadence. |
| `gate.permission-denied-fires` | `PermissionDenied` stays silent through **3** real denials | **Turned 0030's last assumed row into a measurement.** The event-surface run had logged a zero nothing could interpret; this supplies the missing half — an allow control that writes, a deny arm that records three denials in `permission_denials` and writes nothing. With `PermissionRequest` and `Notification`, that is the whole permission-notification surface confirmed absent headless, which is why 0030 says AER *is* the notifier rather than a listener. |

## Effort

| check | result | architectural impact |
|---|---|---|
| `effort.claude-value-set` | claude's `--effort` accepts exactly `{low, medium, high, xhigh, max}` | **Sentinel, not a finding.** 0023's canonical mapping (`quick`/`standard`/`careful`/`exhaustive`) is decided by the vendor's *documented* value set, not a behavioural distinguishability study — this is the re-runnable guard that the set hasn't moved under it. A `FAIL` means a vendor added, removed or renamed a level and the mapping in `vendor-capabilities.md` needs re-deriving before it's trusted again. |
| `effort.agy-value-set` | agy's `--effort` accepts exactly `{low, medium, high}` | Same guard, other vendor. Also the sentinel that keeps the disclosed collapse honest — `agy` has no fourth level, so `careful` and `exhaustive` both resolve to `high`; a `FAIL` here is the first sign that collapse needs re-checking too. |

## Fan-out

| check | result | architectural impact |
|---|---|---|
| `fanout.nesting-allowed-by-default` | one level of nesting runs unconfigured | **Contradicts the vendor's own documentation.** #503 items 4–5 rested on the opposite. AER must set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` explicitly; the gate, cost attribution and concurrency budget must hold for a tree of unknown depth. Lands in M27. |
| `fanout.parent-mode-covers-subagents` | a subagent inherits the parent's mode | AER cannot rely on a subagent being more constrained than its parent. Whatever the lead runs under, the tree runs under. |
| `fanout.concurrency-cap` | peak overlap tracks the cap exactly (2 and 6) | AER can delegate fan-out bounding to the vendor rather than serialising dispatch. **The documented default of 20 remains unverified** — neither arm ran uncapped. |

## Cost

| check | result | architectural impact |
|---|---|---|
| `cost.subagent-tokens-excluded` | top-level `usage.output_tokens` under-reports by 22% on one subagent | Any cost surface summing the top-level field is wrong, and wrong-er with depth. `modelUsage` is the whole-tree figure. Binds J9 (#479). |
| `cost.max-budget-enforced` | `--max-budget-usd` exits 1 with `error_max_budget_usd` | AER can **delegate per-session budget enforcement to the vendor** rather than implementing its own — which pairs with the row above: the vendor is the reliable source for spend, AER's arithmetic over the top-level field is not. |
| `cost.json-schema-conforms` | `--json-schema` constrains the result shape | Makes Architecture Rule 1 practical: Flow routes on a structured return instead of parsing prose. Enables the decision/approval kinds of 0015 without content inspection. |

## Durability and lifecycle

| check | result | architectural impact |
|---|---|---|
| `durability.session-id-guard-is-not-a-lock` | two concurrent processes both win the race | **The docs claim one writer.** `Aer.Daemon` must enforce single-writer per session itself. Amends 0008's premise. |
| `durability.auth-status-is-per-config-root` | `auth status` reports per root, starts no session | **Overturned a recorded constraint.** Per-worker config roots are viable and Rule-4-clean; `auth status` is a free pre-dispatch readiness probe. Corrected Architecture Rule 4 in CLAUDE.md. |
| `durability.config-dir-redirect-breaks-auth` | a fresh `CLAUDE_CONFIG_DIR` is not logged in | The cost of the row above: a fresh root needs a one-time interactive `claude auth login` **by the operator**. That is a human signing in, not AER handling a credential. |
| `durability.agy-home-redirect-isolates-state` | agy under redirected HOME/USERPROFILE populates state without touching `~/.gemini` | **Isolated agy memory scratch space.** Non-shell agy workers redirect HOME/USERPROFILE to state directory (`AER_OUTPUT_DIR` for batch, session directory for daemon sessions), preventing cross-conversation store accumulation without breaking auth. |
| `lifecycle.bg-projection` | `claude agents --json` projects state with a vocabulary | AER can read background session state structurally rather than scraping. **No design change** — confirms 0008's projection model is buildable. |
| `lifecycle.daemon-status` | `claude daemon status` reports machine-readable readiness | Supervisor input for `Aer.Daemon` (#478). **No design change.** |
| `lifecycle.claude-print-reads-prompt-from-stdin` | `claude -p` consumes the prompt from stdin when no positional prompt is given | The #932 enabler: the claude path can take its prompt off the command line, so the `ARG_MAX` ceiling that refuses a large prompt (#598/#612) is liftable for claude via stdin. Control-armed — the prompt-as-argument arm must pass first. |
| `lifecycle.agy-print-requires-prompt-argument` | `agy -p` reads nothing from stdin (prompt or context); the prompt must be the `-p`/`--print` flag value | Makes the #932 fix **vendor-asymmetric**: agy print mode has no off-argv prompt path (no stdin, no `--input-format`, no prompt-file flag), so a large gemini prompt stays `ARG_MAX`-bound until agy grows one. Three-armed (arg echo, context-in-arg positive control, stdin arm). |

## agy

| check | result | architectural impact |
|---|---|---|
| `agy.permissions-are-global-only` | no project-scoped permission rules are honoured | **Hooks are agy's only per-worker gate.** AER cannot scope an agy worker's permissions without writing the operator's own settings file — which it must not. Makes 0029's mandatory hook load-bearing on agy specifically. |
| `agy.force-ask-defeated-by-skip` | `force_ask` collapses under `--dangerously-skip-permissions` | A vendor asymmetry running the *opposite* way from the usual one. It is also the evidence that made measuring agy elicitation non-optional rather than a formality. |
| `agy.hook-deny-honoured` | a `PreToolUse` deny blocks the call | Confirms the gate is symmetric across vendors at the hook layer — the premise 0029's mechanism table depends on, which rests on the deny being *honoured*, not on its reason reaching output. **Corrected:** this row and the check's own description previously read "and surfaces its reason". `agy.broken-hook-fails-open` measured that token absent from agy's output under `-p`, and the check never gated its PASS on it — so a green check was certifying a false half-claim. Scoped to what it tests. |
| `agy.broken-hook-fails-open` | agy also fails open; whether it does so *silently* is unmeasured | **Closed a generalisation, not a gap.** 0029's mandatory self-check was justified from a claude-only measurement while the sentence it supports — "the workspace hook is the only per-worker gate on agy" — is an agy claim. It now rests on an agy run. The consequence is worse here than on claude by exactly the asymmetry `agy.permissions-are-global-only` names: a dead hook on claude still leaves the MCP callback and elicitation; on agy it leaves nothing. The silence half is **not** claimed — no arm gave the output detector a positive control on agy — and the design does not need it, since claude's measured silence already requires the self-check AER runs on every worker. |
| `agy.fails-closed-headless` | `agy -p` auto-denies an ungated tool and names the rule | **No impact** — confirms 0015's corrected premise that both vendors fail closed. Also the reason a permissive arm is required to measure anything else about agy headless. |
| `agy.plan-mode-does-not-deny-writes` | agy writes files under `--mode plan` with no prompt and no refusal, both inside an `--add-dir` path and outside every directory it was given | **Withdrew a generalisation that two doc sites rested on, and exposed a second gap.** `agy.fails-closed-headless` measured one tool — a shell command — and the adapter's paragraph read it as "any tool needing a permission it cannot prompt for", across three modes (gate `claim-scope`). Neither `--mode` nor `--add-dir` bounds a write, so AER's own hook is the only gate on an agy worker's writes, which makes 0029's mandatory hook load-bearing here for the same reason `agy.permissions-are-global-only` already made it. The outside-`--add-dir` arm is the new part and it gates this check's verdict rather than sitting in prose: with a granted write neither hook consults the path at all, so nothing bounds where it lands — filed as #679 rather than patched, because the fix overlaps #669's workspace provisioning. |
| `agy.hook-payload-carries-write-path` | an agy `PreToolUse` payload for `write_to_file` carries `toolCall.args.TargetFile` naming the **absolute** target | **Decides whether #679's fix has an agy half at all.** `AgyHookCheckCommand` decides on `toolCall.name` alone, so confining a granted write to `WorkingDirectory ∪ AER_OUTPUT_DIR` depends entirely on the payload naming a path the hook can resolve — and `agy.plan-mode-does-not-deny-writes` already established that nothing agy offers bounds a write, leaving AER's hook as the only candidate. Measured rather than read off `agy__hooks.md`, because this file records two places that same documentation is wrong (`--cwd` documented and absent, `modelName` present and undocumented), and `agy.hook-env-inherited` only ever reported `toolCall.name` for `run_command` — a different tool, a different field. The absolute form is the load-bearing half: `OutboxPath` will not resolve a relative candidate against the hook's inherited cwd, and agy ignores the process cwd anyway (#472), so a basename would have left nothing to compare against. **Scoped to one of the four `WriteTools`** — the other three are unmeasured, which is why the gate denies a write whose path it cannot find rather than allowing it. Not a sentinel **on the condition that the hook denies when it cannot find a path** — that makes a vendor change loud rather than silent, and the reasoning is void if anyone writes it to allow-on-missing-path. |
| `agy.settings-allow-honoured-headless` | `permissions.allow` is honoured under `-p` | **No impact, and deliberately kept red-adjacent**: upstream #548 says otherwise and does not reproduce here. The check exists to notice if the upstream report becomes true on this host. |
| `agy.settings-allow-write-honoured-headless` | a `write_file(<path>)` allow-rule is honoured under `-p`; without it the write is auto-denied | **Made a write-granted agy role produce output at all (#1084).** A role with `WriteFiles` but no shell/network resolves to `--mode accept-edits`, where agy headless-denies the write tool it cannot prompt for — so `advise`/`orchestrate` on agy wrote nothing, while `implement`/`janitor` worked only because their shell+network grant reaches `--dangerously-skip-permissions`. `AgyWorkerAdapter` now seeds one least-privilege `write_file(<output>)` rule per declared output into the fresh AER-owned home. The rule is **not** the boundary: `agy.hook-deny-holds-under-the-mode-production-uses` measures that AER's `PreToolUse` deny still holds under this exact mode, so the hook (#670) remains what bounds where a write may land — the sibling `command(...)` check proves the *list* loads, this proves the `write_file` *category* is honoured, a distinct claim. Two arms, one variable (rule present vs absent); the without-rule control is the discriminator. |
| `agy.termination-behavior` | `PostInvocation terminationBehavior: terminate` ends the loop | Confirms agy's control surface is real and CLI-reachable. **No design change**; AER does not currently need it. |
| `agy.hooks-load-from-add-dir-not-only-cwd` | agy loads `.agents/hooks.json` from an `--add-dir` directory that is **not** the cwd; hooks in the cwd load **nothing** | **The claim #554's entire gate rests on, and it was fabricated before it was measured.** The adapter cited a check name — `agy.add-dir-grants-files-not-config` — that does not exist; the real neighbouring check, `gate.add-dir-loads-no-config`, is claude-scoped and runs the *opposite* way (`--add-dir` loads no hooks config there). Every other agy hook arm ran `--add-dir wd` with `cwd=wd`, so none could tell "loaded because --add-dir named it" from "loaded because it was the cwd" — while production always has them differ. Measured with three arms: production's arrangement loads and blocks; the cwd-only arm loaded nothing and ran ungated. Had it gone the other way, every agy worker AER spawns would have carried no gate at all, failing open and silent. Sentinel because that failure is invisible from inside AER. |
| `agy.hook-env-inherited` | an agy hook subprocess inherits the environment agy was spawned with | **Makes #554's gate implementable at all.** #543 ships one static settings file per vendor and sends the per-invocation denied-tool list through `AER_HOOK_DENIED_TOOLS`; that only works if the hook process inherits it. claude's corpus states inheritance explicitly, agy's says nothing, so the design could not reuse claude's answer (gate `claim-scope`). Sentinel: losing this fails **silently and totally** — an empty denied list reads as "nothing withheld", every tool is allowed, and the result is indistinguishable from a working gate. |
| `agy.hook-malformed-stdout-fails-open` | unparseable and **empty** hook stdout ALLOW; an unrecognised `decision` *value* DENIES | **Determined `AgyHookCheckCommand`'s failure posture, opposite to its claude sibling.** `HookCheckCommand` used to fail open, argued as "no worse than `--disallowedTools`, which covers the same names"; `agy.permissions-are-global-only` means agy has no such backstop, so anything this hook lets through is ungated outright. **Both now fail closed** — #649 moved the write tools off `--disallowedTools` so the outbox exemption could be reached, which voided the claude-side argument and left the two vendors' postures the same. The asymmetry is the actionable part: a crash, an exception trace, or a silent exit is an **allow**, while a merely wrong verdict string is safe — so the command must emit syntactically valid JSON on every path including its own catch-all handler. Not a sentinel: that conclusion holds whichever way a future agy resolves this, so nothing built on it can rot. |
| `agy.hooks-json-cached-at-startup` | agy reads `.agents/hooks.json` once at startup | **Downgraded a suspected gate bypass to a hygiene concern.** #554 must load its hook from an `--add-dir` directory, and `--add-dir` also grants the worker *write* access there — so the worker can reach the file that gates it. Because agy caches at startup, deleting or rewriting it mid-run does not disable the gate, and `EnsureAgyWorkspace` rewriting on every resolve stops a tampered file surviving into the next spawn **of AER's own hook file**. Scoped deliberately: production also `--add-dir`s the artifacts root and the room's working directory, which AER does not rewrite, and agy's docs place hooks in "`.agents/` in your workspace" — whether agy loads hooks from more than one workspace root, and which handler wins, is unmeasured. Not a claimed bypass; an unclaimed one. Sentinel: an agy that began re-reading per call would silently convert this into a real bypass, with nothing in AER's logs showing a difference. |
| `agy.unlisted-model-acceptance-is-per-name` | **written for #547, NOT YET RUN.** Two prior single-day probes, no shared control between them: `gemini-3-flash` (rc=0, #538) and `gemini-3-pro` (rejected by name, `effort.agy-rejection-is-per-model`) are both unlisted, opposite outcomes. This check reruns both under one catalogued control; whether that confirms "per-name" or shows one of the two has since moved is unresolved until the operator runs it | **Would turn a standing register contradiction into one re-runnable claim, and is the sentinel #547 asked for — once run.** Nothing before this ran both unlisted names under a shared catalogued control, so nothing distinguished "agy accepts unlisted models" from "agy accepts this unlisted model": the population this repo already had was two data points nobody had compared. AER pins a model per worker and its cost/model-attribution surfaces assume the pinned name is the one that ran; this check is what would notice drift in the two probed names (`gemini-3-flash`, `gemini-3-pro`), since nothing else here is positioned to. Which model actually served an accepted request stays unmeasured either way (no attribution surface exists), and "no warning" would be reportable only as "none observed on the captured stream", never as asserted absent. The product-side half — nothing validates a model name a worker binding or a room actually carries — is filed separately as #726 rather than built here, per #547's own "Done when". |
| `prompt.claude-payload-file-execution` | `claude -p` given a short wrapper prompt pointing at a payload file in an `--add-dir` path reads the payload file and executes its contained contract write | **Enables out-of-band prompt delivery for oversize worker prompts (#748).** Prompts exceeding 4,000 chars are written to `{AER_OUTPUT_DIR}/prompt.txt` (%AER_PROMPT_FILE%) and replaced on argv with a concise wrapper prompt. |
| `prompt.agy-payload-file-execution` | `agy -p` given a short wrapper prompt pointing at a payload file in an `--add-dir` path reads the payload file and executes its contained contract write | **Enables out-of-band prompt delivery for oversize worker prompts on agy (#748).** Bypasses the 32k command-line ceiling on Windows and POSIX argument length caps. |

## What this table cannot tell you

- **Whether an implication is correct** — only that one was recorded and is traceable to a run.
- **Whether a finding that changed nothing should have.** A `no impact` row is a judgement. The
  defence is that each names *what* it confirms, so a wrong call is visible rather than silent.
- **What was never measured.** The blind spot is the unrun check, not the unrecorded row. The
  standing ones are listed under *Still not settled* in
  [`vendor-doc-audit.md`](vendor-doc-audit.md); the highest-value is now #531, whether a live
  SEP-1036 url-mode elicitation actually reaches a person and resumes, because it is the only
  finding here that needs a human rather than a better instrument.
- **Whether a result on one vendor holds on the other.** Nothing in this table enforces that, and
  two rows exist only because the question got asked late: `agy.elicitation-capability` and
  `agy.broken-hook-fails-open` were each written after noticing that a claude measurement was
  carrying an agy sentence. `agy.force-ask-defeated-by-skip` is the standing proof that the
  inference is unsafe — the same mechanism, opposite behaviour.

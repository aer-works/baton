[package]
name = "aer-core"
version = "0.6.0"
edition = "2021"
rust-version = "1.74"
license = "Unlicense"
repository = "https://github.com/aer-works/aer-core"
description = "Cross-language process execution and lifecycle supervision runtime"

[lib]
crate-type = ["cdylib", "rlib"]

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_System_Threading",
    "Win32_System_JobObjects",
    "Win32_Foundation",
    "Win32_Security",
] }

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- aer-core .NET binding (P/Invoke wrapper over the FFI boundary), pulled in via the
         external/aer-core submodule per CLAUDE.md's P/Invoke Layer rule. No package feed exists
         for this yet — a single-developer project doesn't need one; `pixi run
         build-core` builds the native lib the ProjectReference below expects. -->
    <ProjectReference Include="..\..\external\aer-core\bindings\dotnet\Aer.Core\Aer.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Lets the test project exercise internal pump machinery (e.g. InFlightExecutionRegistry's
         Register/Unregister/RequestStopAsync) directly where a test needs to force an exact
         internal race deterministically, not just through the public mutation surface. -->
    <InternalsVisibleTo Include="Aer.Flow.Tests" />
  </ItemGroup>

</Project>
